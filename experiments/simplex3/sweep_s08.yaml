# GENERATED BY scripts/gen_simplex3.py -- do not edit by hand.
# Re-run the generator instead; hand edits are lost and, worse, silently diverge
# from the other ~50 files that were meant to agree with this one.
#
# Seed 8 of the simplex sweep: 16 proportions x {1,10,100,1000} = 64 draws.
# Sharded by seed rather than by proportion so each shard still touches all
# three group filters -- source_registry memoises them, so that is three
# filter passes per shard instead of one per draw.
#

name: simplex3_sweep_s08
output_dir: /weka/scratch/cpriebe1/MO/model-taxonomy/results/simplex3
cache_dir: /weka/scratch/cpriebe1/MO/model-taxonomy/results/shared_cache

datasets:
  - name: yahoo_000g1_000g2_100g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 0% g1 / 0% g2 / 100% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_000g1_025g2_075g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 0% g1 / 25% g2 / 75% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 3.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_000g1_050g2_050g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 0% g1 / 50% g2 / 50% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_000g1_075g2_025g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 0% g1 / 75% g2 / 25% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 3.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_000g1_100g2_000g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 0% g1 / 100% g2 / 0% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_025g1_000g2_075g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 25% g1 / 0% g2 / 75% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 3.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_025g1_025g2_050g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 25% g1 / 25% g2 / 50% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 2.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_025g1_050g2_025g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 25% g1 / 50% g2 / 25% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 2.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_025g1_075g2_000g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 25% g1 / 75% g2 / 0% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 3.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_050g1_000g2_050g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 50% g1 / 0% g2 / 50% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_050g1_025g2_025g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 50% g1 / 25% g2 / 25% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 2.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_050g1_050g2_000g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 50% g1 / 50% g2 / 0% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_075g1_000g2_025g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 75% g1 / 0% g2 / 25% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 3.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_075g1_025g2_000g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 75% g1 / 25% g2 / 0% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 3.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_100g1_000g2_000g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 100% g1 / 0% g2 / 0% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas

  - name: yahoo_033g1_033g2_033g3
    recipe_type: class_aware
    n_samples_sweep: tens 3
    seeds: [8]
    # 33% g1 / 33% g2 / 33% g3
    entries:
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [0, 6, 7, 9]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [1, 3, 4]
        class_sampling: pooled           # draw from the union, not per-topic quotas
      - dataset_id: yahoo_answers_topics
        split: train
        weight: 1.0
        text_field: best_answer          # fallback projection; text_fields wins
        text_fields: [question_title, question_content, best_answer]
        text_separator: "\n"
        class_field: topic
        class_filter: [2, 5, 8]
        class_sampling: pooled           # draw from the union, not per-topic quotas

base_models:
  - meta-llama/Llama-3.1-8B

fine_tuning:
  enabled: false

# One (1, 768) centroid per draw. The surrogate is authored, not derived -- the
# full (N, 768) matrix is never stored and `mean` is not invertible -- so adding a
# second representation later means re-embedding all 640.
extraction:
  models: []
  device: cuda
  taxonomies:
    functional:
      enabled: false
    behavioral:
      enabled: false
    dataset_embedding:
      enabled: true
      representation: mean
      embedder:
        model_name: nomic-ai/nomic-embed-text-v1.5
        normalize_embeddings: true
        trust_remote_code: true
        # The literal prefix nomic wants is "search_document: ". Note the existing
        # surrogates spell this `document`, which resolves to the same prefix and
        # the same vectors -- but prompt_name is inside config_dict(), so the two
        # spellings are different embedder_hashes. Reuse this spelling or re-embed.
        prompt_name: search_document
